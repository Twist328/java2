package ru.progwards.java2.lessons.tests.test.calc;

/*import org.junit.*;
import ru.progwards.java2.lessons.tests.calc.*;

public class SimpleCalcMultZero {
    static SimpleCalculator calc;

    @BeforeClass
    public static void init() {
        calc = new SimpleCalculator();
    }

    @Test(expected = ArithmeticException.class)
    public void testMultByZero() {
        int num = 0;
        int num1 = 0;
        /*boolean a=((int)num>=Integer.MIN_VALUE)||((int)num<=Integer.MAX_VALUE);
        boolean b=((int)num>=Integer.MIN_VALUE)||((int)num<=Integer.MAX_VALUE);*/
        //int result = calc.mult(num, num1);
        /*if (result > Integer.MAX_VALUE || result < Integer.MIN_VALUE)
            throw new IntOverflowExc("Int range overflow during on mult " + result);
    }
}*/